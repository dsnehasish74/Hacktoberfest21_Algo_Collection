#include <bits/stdc++.h>
using namespace std;
int main()
{
     ios_base::sync_with_stdio(false);
      cin.tie(NULL);
      cout.tie(NULL);
     
     int n,i;
     cin>>n;
     for(i=2;i<=sqrt(n);i++)
     {
          if(n%i==0)
          {
              cout<<"Not Prime";
              break;
          }
     }
     if(i>sqrt(n))
     cout<<"Prime";
     return 0;
}
